# ci_nurse_mcp_server
mcp server for the CI nurse agent

How to run the server:
  create venv: uv python venv .venv
  install requirements: uv pip install -r requirements.txt
  run server: uv run zmaster.py
  
