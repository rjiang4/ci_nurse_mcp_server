[project]
name = "zeta-master"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "rjiang4", email = "ruixuan.jiang@volvocars.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
zeta-master = "zeta_master:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
